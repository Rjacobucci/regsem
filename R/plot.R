#' Plot function for cv_regsem
#'
#' @param object An object from regsem.
#' @param ... Other arguments.
#'



plot.regsem <- function(object,...){

#  pars_pen <- object[[3]]
#  coef.mat <- object[[1]][,pars_pen]


#  plot(object[[2]][,"lambda"],coef.mat2[,1],ylim=c(min(coef.mat),max(coef.mat)),ylab="Loading",xlab="Penalty",type="l",lty=1,col=1)
#  for(i in 2:length(pars_pen)){
#    lines(object[[2]][conv,"lambda"],coef.mat[,i],lty=i,col=1)
    #points(mat[,(nload)])
#  }
#  abline(a=0,b=0)
}
